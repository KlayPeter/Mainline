# 本地 API 服务端口。Web 开发服务器会把 /api 请求转发到这里。
MAINLINE_PORT=4174

# 留空时使用工作区 data/mainline.sqlite；测试会使用独立内存或临时数据库。
MAINLINE_DATABASE_PATH=

# 第二阶段接入 AI 时由后端读取；绝不暴露给浏览器。
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
