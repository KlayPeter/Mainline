{
  "name": "mainline",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "dev:api": "turbo dev --filter=@mainline/api",
    "dev:web": "turbo dev --filter=@mainline/web",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "check": "turbo typecheck test build"
  },
  "devDependencies": {
    "turbo": "2.10.9"
  }
}
