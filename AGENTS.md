# Agent 工作规约

为了保证代码库的质量与可维护性，AI Agent 在操作本项目时需严格遵循以下规范：
Before reading files:

1. Use CodeGraph MCP first.
2. Use codegraph_explore to understand architecture.
3. Use codegraph_impact before modifications.
4. Avoid scanning the repository with grep.
5. Read only files returned by CodeGraph.

## 功能文档强制同步规约

本项目用功能代码 Owner 目录下的 `FEATURE.md` 记录目标、实现链路、代码边界和验证方式，具体位置由 `docs/features/README.md` 统一索引。`AGENTS.md` 只存放 AI 开发规约；不要把业务说明分散到新的 `AGENTS.md` 中。

每次修改功能时必须执行以下步骤：

1. 修改前先查看 `docs/features/README.md`，根据“代码路径 → 功能文档”映射找到所有受影响的 `FEATURE.md`。
2. 阅读对应 `FEATURE.md`，再使用 CodeGraph 检查实际调用链和影响范围。文档与代码冲突时，以代码和测试为证据，并在本次改动中修正文档。
3. 功能代码与对应 `FEATURE.md` 必须在同一个提交中更新。行为或契约变化时同步新增/修改测试；行为不变时运行现有回归测试，并核对更新代码地图、测试地图或变更记录，禁止为了满足形式要求制造无意义的测试改动。
4. 流程、状态、Agent/Tool/Skill 调用关系、数据归属或外部系统交互发生变化时，必须同步更新对应 `FEATURE.md` 内的文字流程和内联 Mermaid；Mermaid 直接由 Markdown 渲染，禁止额外生成 PNG 或独立 `.mmd` 文件。
5. 共享平台改动可能影响多个功能。修改 `MessageRouter`、`ContextRouter`、`AgentHost`、`ScheduledJobs`、公共 Store、配置结构或公共 Adapter 时，必须更新映射表列出的每一份受影响功能文档。
6. 新增功能时，先在功能代码的主 Owner 目录中复制 `docs/features/FEATURE_TEMPLATE.md` 为 `FEATURE.md`，再把它加入 `docs/features/README.md`。跨目录功能只能有一个主 Owner，其他实现位置写入代码地图；没有功能文档和验证说明的新功能不得提交。
7. 提交前检查文档中的源文件、接口、配置、Agent ID、Capability 和测试文件都真实存在；运行与改动相称的测试，并执行 `git diff --check`。

`FEATURE.md` 是“功能能力卡”，不等于运行时 Agent Manifest。只有需要独立目标、模型循环、会话、暂停恢复或独立部署的能力才实现为 Agent；强一致状态继续由领域服务拥有。


1. **每次修改必提交 (Commit)**
   所有代码的修改（包括新增功能、修复 Bug、重构等）在完成一个独立节点后，都必须进行对应并有意义的 `git commit`。

2. **提交前必测试 (Test Before Commit)**
   修改完毕的代码在执行 commit 前，必须进行充分的测试，比如测试eslint 跑功能等等。只有在确认测试无误、项目能正常运行的情况下，才能将代码提交至代码库。


   减少常见LLM编码错误的行为准则。可根据需要与项目特定指令合并。
**权衡：**这些准则倾向于谨慎而非速度。对于简单任务，自行判断即可。
## 1. 先思考再写代码
**不要假设。不要隐藏困惑。把权衡摆到台面上。**
在动手实现之前：
- 明确说出你的假设。不确定就问。
- 如果存在多种理解方式，全部列出来——不要默默选一个。
- 如果有更简单的方案，说出来。该反驳就反驳。
- 如果有什么不清楚的，停下来。说明哪里让你困惑。提问。
## 2. 简洁优先
**用最少的代码解决问题。不写投机性代码。**
- 不加超出需求的功能。
- 一次性代码不搞抽象。
- 没人要求的"灵活性"和"可配置性"不要加。
- 不要为不可能出现的场景写错误处理。
- 如果你写了200行但50行就能搞定，重写。
问自己一句："一个资深工程师会说这写复杂了吗？"如果是，简化。
## 3. 精准修改
**只动必须动的地方。只清理自己制造的问题。**
编辑已有代码时：
- 不要顺手"改进"旁边的代码、注释或格式。
- 没坏的东西不要重构。
- 匹配现有风格，即使你会用不同的写法。
- 如果注意到不相关的死代码，提一嘴就好——别删。
当你的修改产生了孤立代码时：
- 移除因你的改动而变成未使用的import、变量和函数。
- 不要动原本就存在的死代码，除非被明确要求。
检验标准：每一行改动都应该能直接追溯到用户的需求。
## 4. 目标驱动执行
**定义成功标准。循环验证直到确认通过。**
把任务转化为可验证的目标：
- "加验证"→"为非法输入写测试，然后让测试通过"
- "修这个bug"→"写一个能复现它的测试，然后让测试通过"
- "重构X"→"确保重构前后测试都能通过"
对于多步骤任务，列出简要计划：
1. [步骤]→验证：[检查项]
2. [步骤]→验证：[检查项]
3. [步骤]→验证：[检查项]
强成功标准让你能独立循环推进。弱标准（"让它能跑"）则需要不断澄清。
---
**这些准则起作用的标志是：**diff中不必要的改动更少了，因过度复杂化而返工更少了，澄清性问题出现在实现之前而不是犯错之后。

## 本项目架构与质量门禁

- 工作区固定为 `apps/web`、`apps/api`、`packages/contracts`，业务逻辑不得堆在路由处理器或 Vue 页面中。
- 前端业务数据以 API 为事实来源；Pinia 只保存会话和跨页面 UI 状态，禁止持久化文案正文或会话 Cookie。
- 后端使用 TypeBox 作为运行时输入边界；SQL、外部命令和第三方返回值必须在领域服务或 Adapter 中收口。
- 采集元数据与媒体处理必须是两个独立动作；任何定时任务不得自动创建媒体任务。
- 新增依赖前说明必要性；优先使用平台标准库和现有依赖。

CodeGraph 使用顺序：

1. 首次进入项目执行 `codegraph init -i`；已有索引执行 `codegraph sync`。
2. 修改前使用 `codegraph_explore` 和 `codegraph_impact`。
3. 结构调整后执行 `codegraph index`，再用 `codegraph status` 确认索引最新。
4. 提交前执行 `codegraph affected` 辅助选择回归测试，不能用它替代类型检查和完整测试。

每个阶段至少执行：类型检查、相关单元测试、构建、`git diff --check`。涉及 UI 时增加 Playwright 与可访问性验证；涉及外部媒体工具时增加固定样例回归。
